//! Executable contract checks for the completed Tasks solution.
//!
//! This crate ships no library code of its own; it exists so its integration
//! tests can run the shared `tests/contracts` suite against `tasks-solution`.
//! It is the reference proof that the finished solution satisfies every
//! milestone contract the starter must eventually meet.
